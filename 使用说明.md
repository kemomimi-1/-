# 🧠 EEG-AI 智能脑电分析平台 — 使用说明

本指南帮助你从零开始配置环境并成功运行本项目。本项目是一个基于 **Spring Boot + InfluxDB + MySQL + 阿里云通义千问大模型** 的脑电波实时采集、分析与 AI 智能交互平台。

---

## 📋 目录

1. [软件环境要求](#1-软件环境要求)
2. [数据库配置](#2-数据库配置)
3. [启动后端项目](#3-启动后端项目)
4. [使用 OpenBCI GUI 发送脑电数据](#4-使用-openbci-gui-发送脑电数据)
5. [前端页面说明与操作流程](#5-前端页面说明与操作流程)
6. [AI 智能助手功能详解](#6-ai-智能助手功能详解)
7. [实时监测页面](#7-实时监测页面)
8. [会话管理系统](#8-会话管理系统)
9. [常见问题排查](#9-常见问题排查)
10. [项目技术架构总览](#10-项目技术架构总览)

---

## 1. 软件环境要求

在开始之前，请确保你的电脑安装了以下环境。**请务必按照顺序安装：**

| 名称 | 版本要求 | 用途 | 下载建议 |
|------|---------|------|---------|
| **操作系统** | Windows 10 / 11 | — | — |
| **JDK** | **21**（必须是 21） | Spring Boot 运行环境 | Oracle 官网 或 清华镜像源，安装后配置 `JAVA_HOME` 环境变量 |
| **MySQL** | 8.0+ | 存储用户信息、会话记录、对话历史 | 推荐直接下载安装包，记住你设置的 root 密码 |
| **InfluxDB Core** | 3.2.1 | 存储高频时序脑电波数据（Raw / Filtered / BandPower） | 下载 Windows 压缩包解压即可 |
| **OpenBCI GUI** | 最新版 | 脑电设备 / 模拟器，通过 UDP 发送脑电数据 | 官网下载 |
| **IDE** | IntelliJ IDEA（社区版即可） | 代码编辑与运行 | JetBrains 官网 |
| **浏览器** | Chrome / Edge（最新版） | 前端页面展示 | — |

---

## 2. 数据库配置

### A. MySQL 设置

1. 启动你的 MySQL 服务。
2. 使用数据库工具（Navicat / DataGrip / 命令行均可）登录 MySQL。
3. **创建数据库**（项目也支持自动建库，这步可选但推荐手动执行）：
   ```sql
   CREATE DATABASE eeg_platform DEFAULT CHARACTER SET utf8mb4;
   ```
4. **导入数据表**：项目根目录下附带了 `eeg_platform.sql` 文件，用你的数据库工具导入该文件即可完成建表和初始数据导入。
5. **修改项目数据库密码**：
   - 用 IDEA 打开项目后，找到 `src/main/resources/application.yml`
   - 找到 `spring.datasource.password` 配置项
   - 把默认密码改成**你自己电脑的 MySQL root 密码**
   ```yaml
   datasource:
     url: jdbc:mysql://localhost:3306/eeg_platform?createDatabaseIfNotExist=true&...
     username: root
     password: 你的MySQL密码    # ← 改这里
   ```

> 💡 **注意**：项目使用 JPA + Hibernate，配置了 `ddl-auto: update`，即使不手动导入 SQL 文件，启动时也会**自动建表**。但导入 SQL 文件可以获得预置的初始数据。

### B. InfluxDB 设置

1. **准备 InfluxDB**：下载 InfluxDB Core 3.2.1 的 Windows 压缩包并解压。

2. **启动 InfluxDB**：在解压目录打开命令行（CMD），输入以下命令启动：
   ```bash
   influxdb3.exe serve --node-id mynode --object-store file --data-dir ./influxdb-data
   ```
   > 参数说明：
   > - `--node-id mynode` — 节点名称
   > - `--object-store file` — 数据存储在本地文件系统
   > - `--data-dir ./influxdb-data` — 数据目录（自动生成）

3. 命令行窗口会输出日志，**不要关闭它**，说明数据库正在运行。
4. 如果有 Windows 防火墙提示，点击 **"允许访问"**。
5. 确保运行在默认端口 **`8181`** 上（窗口无报错即正常）。

> 💡 **注意**：API Token 和连接配置已内置在 `application.yml` 中，你**只需要启动 InfluxDB 服务**，不需要打开浏览器去额外配置。项目 `项目运行指南` 文件夹中也附带了启动命令和 Token 参考信息。

---

## 3. 启动后端项目

1. 打开 IntelliJ IDEA。
2. 选择 `File` → `Open`，找到并选中以下目录并打开：
   ```
   ai_eeg-main/ai_eeg-main/source/eeg-fileData
   ```
3. IDEA 右下角会提示加载 Maven 项目，点击同意，耐心等待依赖下载完成（首次可能需要 3~5 分钟）。
4. 在左侧项目结构树中找到启动类：
   ```
   src/main/java/com/eeg/EegFileDataApplication.java
   ```
5. 右键点击该文件 → 选择 **Run 'EegFileDataApplication'**。
6. 控制台出现类似以下日志表示启动成功：
   ```
   Started EegFileDataApplication in x.xxx seconds
   ```

> ⚠️ **启动前确保**：MySQL 服务已启动、InfluxDB 已运行（命令行窗口不能关）。

---

## 4. 使用 OpenBCI GUI 发送脑电数据

本项目通过 **UDP 协议** 接收脑电数据报文。操作步骤如下：

### 步骤一：启动 OpenBCI GUI

1. 打开 OpenBCI GUI（使用项目附带的或官网下载的 `OpenBCI_GUI.exe`）。
2. 页面左上角选择数据源 → 选择 **SYNTHETIC (algorithmic) 8chan（模拟数据）** → 点击 `START SESSION`。

### 步骤二：配置 Networking

1. 在右下角的下拉选择框（默认显示 Accelerometer）中，选择 **Networking**。
2. 在 Networking 面板中配置 **3 个 UDP 数据流**：

| 流序号 | Protocol | IP Address | Port | Data Type |
|-------|----------|------------|------|-----------|
| Stream 1 | `UDP` | `127.0.0.1` | 页面分配的 Raw 端口（如 `15001`） | **TimeSeriesRaw** |
| Stream 2 | `UDP` | `127.0.0.1` | 页面分配的 Filt 端口（如 `15002`） | **TimeSeriesFilt** |
| Stream 3 | `UDP` | `127.0.0.1` | 页面分配的 Band 端口（如 `15003`） | **BandPower** |

> ⚠️ **注意**：IP、Port、Data Type 的顺序和类型必须严格按照上表配置，否则数据将无法正确解析！

> 💡 **端口号来源**：登录网页后，在右侧智能控制面板的 "🔗 连接控制" 中点击 **"🚀 请求连接"**，页面会自动分配 3 个端口并展示给你。

### 步骤三：开始发送数据

1. 点击 Networking 面板下方的 **Start UDP Stream**。
2. 点击 OpenBCI GUI 左上角的 **Start Data Stream**。
3. 回到网页，展开右侧的 "🌊 数据流状态" 面板，可以看到数据包计数器开始增长。

### 步骤四：停止数据传输

> ⚠️ **停止顺序**：先在前端网页点击 **"🔌 断开连接"**，然后再从 OpenBCI GUI 停止数据传输。

---

## 5. 前端页面说明与操作流程

后端启动成功后，打开浏览器：

### 5.1 登录页面

- 访问 `http://localhost:8080/login.html`
- 支持 **注册 / 登录** 功能
- 首次使用请点击注册，创建一个账号

### 5.2 主界面

- 登录成功后自动跳转到 `http://localhost:8080/index.html`
- 顶部有两个标签页可切换：

| 标签页 | 功能 |
|-------|------|
| **AI 对话** | AI 智能助手对话窗口（默认页面） |
| **实时监测** | 实时脑电频段数据监测仪表盘 |

### 5.3 AI 对话页面布局

| 区域 | 位置 | 功能说明 |
|------|------|---------|
| **左侧边栏** | 左侧 | 用户信息、新建对话、对话历史列表（支持收藏/删除/筛选） |
| **中央聊天区** | 中间主区域 | AI 智能助手对话窗口 |
| **右侧控制面板** | 右侧（可折叠） | 连接控制、数据流状态、会话管理、OpenBCI 配置说明 |

### 5.4 操作流程

```
注册/登录 → 右侧面板点击"🚀 请求连接"获取端口 → OpenBCI 配置 UDP 发送 
→ 顶部点击"▶ 开始分析" → 切换"实时监测"查看数据 → 切回"AI 对话"与 AI 分析脑电
```

---

## 6. AI 智能助手功能详解

中央聊天区域是 AI 智能助手，基于**阿里云通义千问大模型（qwen-max）**，配备智能工具协作系统 v3.0。

### 6.1 基础功能

- **自然语言提问**：直接用中文提问关于脑电数据的任何问题
- **SSE 流式输出**：AI 回复以 Server-Sent Events 流式输出，实时显示思考状态和回复内容
- **思考状态展示**：AI 在思考和调用工具时，界面会显示当前正在执行的操作（如"正在调用工具: getSessionHistory"）

### 6.2 智能 MCP 工具链

AI 可以自主调用多个 **MCP（Model Context Protocol）工具** 查询实时脑电数据，包括：

| 工具类别 | 能力 |
|---------|------|
| **数据查询** | 查询原始/滤波/频段功率时序数据 |
| **会话管理** | 获取活跃会话、历史记录、数据量评估 |
| **信号质量** | 实时信号质量监控、数据完整性检查 |
| **高级分析** | 自定义 InfluxQL 查询、会话间对比分析、综合报告生成 |

AI 在回复结束后，会在消息底部显示本次调用了哪些工具（工具标签）。

**使用示例**：
```
"分析一下我最近的 Alpha 波趋势"
"我的脑电信号质量怎么样？"
"对比一下最近两次会话的数据"
"帮我查询最近 5 分钟的 Beta 频段功率数据"
"我现在是什么脑电状态？"
```

### 6.3 对话管理

- **新建对话**：点击左侧边栏的 **"+ 新建对话"** 按钮
- **历史对话**：左侧边栏列出历史对话列表，点击可切换回之前的对话
- **收藏对话**：点击对话项右侧的书签图标 🔖 可收藏/取消收藏
- **筛选对话**：顶部有 "全部" / "收藏" 两个筛选按钮
- **删除对话**：点击对话项右侧的垃圾桶图标 🗑 可删除
- **清除对话**：顶部 "清除对话" 按钮可清空当前聊天界面

---

## 7. 实时监测页面

点击顶部导航的 **"实时监测"** 标签页切换到监测仪表盘。

### 7.1 状态概览

页面顶部显示当前分析状态：
- 🔴 **等待数据** — 尚未开始分析
- 🟢 **分析中** — 正在实时处理脑电数据

### 7.2 五大频段卡片

页面显示 5 个频段的实时数据卡片：

| 频段 | 频率范围 | 含义 |
|------|---------|------|
| 🌙 **Delta** | 0.5-4 Hz | 深度睡眠 / 身体修复 |
| 🧘 **Theta** | 4-8 Hz | 冥想 / 创造力 / 浅睡 |
| 😌 **Alpha** | 8-13 Hz | 放松 / 平静 / 静息状态 |
| 🎯 **Beta** | 13-30 Hz | 专注 / 逻辑思维 / 警觉 |
| ⚡ **Gamma** | 30-100 Hz | 高度认知 / 学习 / 记忆 |

每个卡片都有进度条和数值展示，点击 **"?"** 按钮可查看该频段的详细说明。

### 7.3 分析记录时间线

页面底部显示分析记录时间线，每次分析完成后会生成一条记录，方便回顾历史分析结果。

### 7.4 弹幕通知系统

- 页面右下角有一个 **悬浮球** 💬，点击可打开实时分析历史记录侧边栏
- 当有新的分析结果时，悬浮球会显示红色角标提醒

---

## 8. 会话管理系统

- 访问 `http://localhost:8080/session-management.html` 或从右侧面板中点击进入
- 支持查看所有 EEG 采集会话的详细信息：
  - 会话时间、持续时长
  - 各数据流状态（Raw / Filtered / BandPower）
  - 数据包总量统计
  - 会话状态（活跃 / 已完成 / 已取消）

---

## 9. 常见问题排查

### Q: 启动项目报数据库连接失败？
✅ 检查 MySQL 服务是否启动 → 检查 `application.yml` 中的密码是否改成你自己的。

### Q: 页面打开是空白 / 样式异常？
✅ 按 `Ctrl + Shift + R` 强制刷新浏览器缓存。

### Q: 连接后看不到数据？
✅ 确认以下顺序：
1. InfluxDB 命令行窗口在运行
2. 网页已点击 "🚀 请求连接" 获取端口
3. OpenBCI GUI 中 IP 为 `127.0.0.1`，端口与页面显示的一致
4. OpenBCI 已点击 "Start UDP Stream" **和** "Start Data Stream"
5. Data Type 顺序正确：TimeSeriesRaw → TimeSeriesFilt → AvgBandPower

### Q: AI 回复一直显示"正在思考"不出来？
✅ 检查网络连接（需要访问阿里云 API）→ 等待 10~30 秒（AI 调用工具链可能较慢）→ 如果超过 1 分钟无响应，刷新页面重试。

### Q: 实时监测页面没有数据？
✅ 确保已点击顶部的 **"▶ 开始分析"** 按钮，且右侧面板的数据流状态显示有数据包在接收。

### Q: InfluxDB 启动报错？
✅ 确保在 `influxdb3.exe` 所在目录打开命令行，并完整输入启动命令：
```bash
influxdb3.exe serve --node-id mynode --object-store file --data-dir ./influxdb-data
```

### Q: 端口冲突 / 端口不可用？
✅ 项目使用端口池 `15001~65535` 自动分配，确保这些端口没有被其他程序占用。可以点击 "⚡ 强制结束会话" 释放端口后重新连接。

### Q: 停止数据传输的正确顺序？
✅ **先在网页点击 "断开连接"**，然后再在 OpenBCI GUI 中停止传输。顺序反了可能导致端口无法正确释放。

---

## 10. 项目技术架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                    前端 (纯 HTML/CSS/JS)                      │
│  login.html → index.html (AI对话 + 实时监测双页面)            │
│                 session-management.html                      │
│  JS 模块: globals / api / auth / connection / chat /         │
│           barrage / session / utils / app                    │
├─────────────────────────────────────────────────────────────┤
│                 Spring Boot 后端 (JDK 21)                    │
│                                                             │
│  Controller 层:                                              │
│    AuthController              → 用户注册 & 登录认证          │
│    EEGConnectionController     → 设备连接 & 端口分配管理      │
│    AIQueryController           → AI 查询 & SSE 流式输出      │
│    ConversationHistoryController→ 对话历史 CRUD               │
│    SessionManagementController → EEG 会话管理                │
│    RealTimeAnalysisController  → 实时频谱分析控制             │
│    MCPAnalysisController       → MCP 高级分析接口             │
│    DataQueryController         → 数据查询接口                 │
│                                                             │
│  Service 层:                                                 │
│    EEGDataReceiver              → UDP 数据包接收              │
│    InfluxDBService              → InfluxDB 时序数据读写       │
│    AIModelService               → 通义千问 API 调用           │
│    AIStreamService              → SSE 流式输出管理            │
│    MCPToolRegistry              → MCP 工具注册与执行          │
│    RealTimeSpectrumAnalysis...  → 实时 FFT 频谱分析          │
│    ConversationHistoryService   → 对话历史记录管理            │
│    EEGSessionService            → EEG 会话生命周期管理        │
│    EnhancedPortAllocationService→ 智能端口分配 & 回收         │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  数据存储层                                                   │
│    MySQL 8.0+     → 用户、会话、对话历史等结构化数据           │
│    InfluxDB 3.x   → 原始/滤波/频段功率 时序脑电数据           │
├─────────────────────────────────────────────────────────────┤
│  外部服务                                                     │
│    阿里云通义千问 (qwen-max)  → AI 大模型推理                 │
│    OpenBCI GUI               → 脑电设备/模拟器 UDP 数据源     │
└─────────────────────────────────────────────────────────────┘
```

### 关键技术特性

| 特性 | 说明 |
|------|------|
| **SSE 流式输出** | AI 回复通过 Server-Sent Events 实时推送到前端，支持分类事件（thinking/content/tools/done/error） |
| **MCP 工具协作** | AI 可自主决策调用多个数据分析工具，支持智能协作策略、复杂度评估和动态工具选择 |
| **实时频谱分析** | 基于 FFT 的实时脑电频段功率分析，10 秒间隔自动分析，结果推送到监测仪表盘 |
| **WebSocket 弹幕** | 实时分析结果通过 WebSocket 推送为弹幕通知 |
| **动态端口分配** | 每用户独立 3 端口（Raw/Filt/Band），自动分配与超时回收 |
| **对话收藏管理** | 支持对话收藏、筛选、删除，完整的对话历史 CRUD |
| **智能数据处理** | 自动处理大数据量（>4MB）的工具结果，智能摘要压缩 |
| **多级错误恢复** | AI 请求支持最多 3 次重试，指数退避策略 |

### 项目目录结构

```
ai_eeg-main/
├── eeg_platform.sql              ← 数据库初始化 SQL
├── doc/                          ← 项目文档
├── 项目运行指南/                   ← 附带的启动指南和截图
│   ├── 项目启动.md
│   ├── influxDB-3.2.1启动命令行/
│   ├── influxdb3.2.1的token/
│   └── pic/                      ← 操作截图
└── source/eeg-fileData/          ← Spring Boot 项目源码
    └── src/main/
        ├── java/com/eeg/         ← Java 后端代码
        │   ├── config/           ← 配置类（AI模型、安全等）
        │   ├── controller/       ← REST 控制器（9个）
        │   ├── entity/           ← 数据实体类
        │   ├── repository/       ← 数据访问层
        │   ├── service/          ← 业务逻辑层（12个服务）
        │   └── utils/            ← 工具类
        └── resources/
            ├── application.yml   ← 核心配置文件
            └── static/           ← 前端静态资源
                ├── css/          ← 样式
                ├── js/           ← JavaScript 模块（9个）
                ├── index.html    ← 主页面（AI对话 + 实时监测）
                ├── login.html    ← 登录注册页面
                └── session-management.html ← 会话管理页面
```

---

> 
